<UserControl x:Class="WPF_TOPClassWork.View.Avatar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_TOPClassWork.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderThickness="1" Width="{Binding Size}" Height="{Binding Size}"  BorderBrush="Black" CornerRadius="50" 
        HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid >
            <Border Name="mask" Background="White" CornerRadius="50" />
            <StackPanel>
                <StackPanel.OpacityMask>
                    <VisualBrush Visual="{Binding ElementName=mask}"/>
                </StackPanel.OpacityMask>
                <Image Source="{Binding PathIcon}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
